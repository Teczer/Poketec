version: "3.8"

services:
  spalliandex:
    container_name: spalliandex
    image: spalliandex:latest
    env_file: ./.env
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - ${PORT}:80
    environment:
      - NODE_ENV=${NODE_ENV}
      - PORT=${PORT}
    restart: unless-stopped
    # Limites de mémoire pour éviter l'OOM-Killer
    deploy:
      resources:
        limits:
          memory: 128M
        reservations:
          memory: 32M
    # Alternative pour docker-compose v2 (sans Swarm)
    mem_limit: 128m
    memswap_limit: 128m
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--no-verbose",
          "--tries=1",
          "--spider",
          "http://localhost:80/",
        ]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
